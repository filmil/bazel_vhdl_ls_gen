# Instructions for Gemini

- Use **Conventional Commits** for commit messages.
- Use `bazel build //...` to build the codebase.
- Use `bazel test //...` to run tests.
- Read `README.md` for general instructions.
